<?xml version="1.0" encoding="utf-8"?>
<!-- Launch screen: solid felt-dark background with the launcher icon centered.
     Deliberately minimal so the transition into Flutter's UI is seamless. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/rankRushBackground"/>
    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher"/>
    </item>
</layer-list>
